cmake_minimum_required(VERSION 3.16.0)
if(DEFINED ENV{APP_VERSION} AND NOT "$ENV{APP_VERSION}" STREQUAL "")
  set(PROJECT_VER "$ENV{APP_VERSION}")
else()
  set(PROJECT_VER "0.0.0-dev")
endif()
include($ENV{IDF_PATH}/tools/cmake/project.cmake)
project(tesla_fsd_controller)
